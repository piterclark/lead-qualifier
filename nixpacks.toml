[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["PLAYWRIGHT_BROWSERS_PATH=/app/playwright-browsers playwright install chromium"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
